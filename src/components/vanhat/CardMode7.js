import React from 'react'
import { Animated, TouchableOpacity, PanResponder } from 'react-native'



const CardMode7 = ({ index, card, position, rightPackPosition, unitWidth, bufferLeft, handleMoveToLeftGamePack }) => {
    return (
        <TouchableOpacity>

        </TouchableOpacity>
    )
}

export default CardMode7


